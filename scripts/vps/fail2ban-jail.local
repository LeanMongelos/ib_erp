# Fail2ban — iBiomédica ERP (copiar a /etc/fail2ban/jail.d/ibiomedica.local)
#
# Protege SSH contra brute-force. Ajustar port si DonWeb usa otro puerto.

[sshd]
enabled = true
port = 5244
filter = sshd
logpath = /var/log/auth.log
maxretry = 5
findtime = 10m
bantime = 1h
